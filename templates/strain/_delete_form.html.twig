<form method="post" action="{{ path('strain_delete', {'id': strain.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce résultat ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ strain.id) }}">
    <button class="btn btn-danger" class="btn">Supprimer</button>
</form>
